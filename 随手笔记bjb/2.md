1.

Rust 并不会尝试⾃动地将⾮布尔值转换为布尔值



2. 

```rust
fn main(){

    let condition = true;
    let mut my_number = if condition {2} else {3};
    println!("{}",my_number);
}
```





3. 

copy 属性

所有整数类型，⽐如 u32。

布尔类型，bool，它的值是 true 和 false。

所有浮点数类型，⽐如 f64。

字符类型，char。

元组，当且仅当其包含的类型也都实现 Copy 的时候。⽐如，(i32, i32) 实现了 Copy，但 (i32, String) 就没有





4. 

```rust
fn main(){
    let s1 = String::from("hello world");
    let s2 = s1;
    print!("{}",s1);
}
```

s1是开辟在堆上面的

所以当执行了s2 = s1 就会发生下面的情况

![image-20260101115326711](E:\LosNote\随手笔记bjb\png\image-20260101115326711.png)



5. 

对于知识点3 
就可以栈上进行拷贝



为什么叫做资源获取即初始化






