1. 

```c++
QListWidget
如何 通过 程序 主动选择其中一个？
```

- ```c++
  for (int i = 0; i < ui->listWidget->count(); ++i) {
      QListWidgetItem* currentItem = ui->listWidget->item(i);
      // 取出自定义数据，并转换为整数进行比较
      if (currentItem->data(Qt::UserRole).toInt() == targetId) {
          // 找到了！选中它
          ui->listWidget->setCurrentItem(currentItem);
          // 找到了就可以退出循环了
          break; 
      }
  }
  ```

  - 这样取出来





2. 

```c++
  connect(ui->templatesList, &QListWidget::itemClicked, this, [=](QListWidgetItem* item) {
      QString path = item->data(Qt::UserRole).toString();
      HS_imagePath = path;
      match(HS_imagePath);

      ui->templatesList->setCurrentItem(item);
  });


```

- 直接这么设置就行了



3. 

```c++
imgBtn->setUserData(Qt::UserRole, fullPath);

这样不行么？
```

- `QPushButton` 没有 `setUserData` 函数



imgBtn->setProperty("fullPath", fullPath);

- 取出来就是 QString path = btn->property("fulllpath").toString();



4. [Qt]

如何实现在一个界面 点击一个按钮 把另一个界面显示出来

还可以自动销毁

```c++

/// <summary>
/// 颜色按钮的点击
/// </summary>
void StepThreeWidget::onColorBtnClicked()
{
    HS_colorChannal->show();

    if (HS_colorChannal->isMinimized())
    {
        HS_colorChannal->showNormal();
    }

}
```

- 如果是 最小化了 
- 那么 我们可以把这个变成 最大化

- [ ] 1_19





5. 

QImage.isNull()
如何理解？



- 这张图片是不是 一个空照片
- 没被分配内存的时候 为true















