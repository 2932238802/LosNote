1. 如何隐藏水平滚动条

- ui->musicList->setHorizontalScrollBarPolicy(Qt::ScrollBarAlwaysOff);

- 可以设置对应的策略



2. 
```c++
#define Log(fmt,...) printf("[%s:%d]" fmt, __FILE__,__LINE__,__VA_ARGS__);
```

- C/C++ 编译器 有一个特性 就是会自动拼接 相邻的字符串字面量

```c++
#include <stdio.h>

#define Log(fmt, ...) printf("[%s:%d]" fmt "\n", __FILE__, __LINE__, ##__VA_ARGS__);
int main()
{
    Log("hello world!");

    int i = 10;
    Log("i的值是:%d", i);
    return 0;
}
```


3. #define _GNU_SOURCE 的意义
- 是 GNU C 函数特有的  不属于C语言标准和Posix标准的

4. stackedWidget
如何 获取 当前 显示的 ui的组件？


5. 

这样一个信号
onUpdataLikeMusicAndPage
这个函数 内部 可以 怎么调用 知道 是哪一个发出来的？







