1. [rust]

```rust
fn main(){
    let add = |x:u16| {
        2*x
    };
    let mut number:u16 = add(2);
    number = number + 1;
    println!("{}",number);
}
```



- | | {}
- 匿名函数的使用
- 输出是5



2. 

问：取出 QComboBox 的各个组件的信息

QString savePath = ui->qualifiedImageCB->itemData(index).toString();



- 使用 itemData





3. 

```rust
fn main(){
    let guess:usize = "42".parse().expect("解析失败...");
    println!("{}",guess);
}
```

- rust 进行解析

- [ ] 1_16







4.  [rust]

- 数组存放在 栈上面而不是 堆上面

- [ ] 1_16





5. [rust]

- 数组的表示 [类型 ; 个数]

























