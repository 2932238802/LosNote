1. 

如何 获取 QComboBox的选中的组件的信息
比如 选中了 其中的某一个 触发 一个 信号
选择另一个 触发另一个？



-  `currentIndexChanged`
   -  只要当前选中的项 变了 不管是用户选的 还是 程序选的 都会触发
-  ui->QComboBox->currentIndex();
   -  获取当前的索引
-  ui->QComboBox-> currentText();
   -  获取当前的 内容
-  





2. 

拿到 一个 QJsonArray
如果要解析里面的数据
我们第一步要做什么？

- 