1. 

```c++
item->setFlag(QGraphicsItem::ItemIsMovable);
    item->setFlag(QGraphicsItem::ItemIsSelectable);
    item->setFlag(QGraphicsItem::ItemIsFocusable);
```

如何 理解 这三个 指令？

答：

- ItemIsMovable 鼠标移动 让用户能够 自由布局 场景中的元素
- ItemIsSelectable 允许用户 指定或操作的一个或多个目标
- ItemIsFocusable 接受鼠标事件 让元素 响应 鼠标输入

- [ ] 11_25





2. 

撤回 逻辑 需要自己去实现吗 
还是 Qt 里面又内置的 ui撤回逻辑？

有撤回逻辑 但是 每一种原子操作 都需要 自己手动去实现undo 和 redo的具体逻辑

- [ ] 11_25





3. 

```c++
#include<QGraphicsScene>

class LosTool : public QObject
{
    Q_OBJECT
public:
    explicit LosTool(QObject *parent = nullptr);
    virtual ~LosTool() = default;

private:
    virtual void mousePressEvent(QMouseEvent *event, QGraphicsScene *scene) = 0;
    virtual void mouseMoveEvent(QMouseEvent *event, QGraphicsScene *scene) = 0;
    virtual void mouseReleaseEvent(QMouseEvent *event, QGraphicsScene *scene) = 0;

signals:
};


class Pen: public LosTool{
 

};

```



满足条件public 
Pen 会继承这个 Q_OBJECT 属性嘛？

![image-20251124135243368](png/image-20251124135243368.png)



- [x] 11_25





4. 

Qt 透明度 通常用 表示 取值范围是 0.0 ~ 1.0 

- [x] 11_25





5. 





























